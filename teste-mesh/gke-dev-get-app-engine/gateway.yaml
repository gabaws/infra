apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: dev-get-test
  namespace: dev-get-test
spec: 
  selector:
    istio: istio-ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
      - "*"