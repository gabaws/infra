# Exemplo de arquivo backend.tf
# Copie este arquivo para backend.tf e configure com seus valores
# OU configure diretamente no versions.tf

terraform {
  backend "gcs" {
    bucket = "terraform-state-bucket-seu-projeto"
    prefix = "terraform/state"
  }
}

