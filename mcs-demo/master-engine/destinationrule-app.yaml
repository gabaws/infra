apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: hello-app-engine
  namespace: mcs-demo
spec:
  host: hello-app-engine.mcs-demo.global
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
    loadBalancer:
      simple: ROUND_ROBIN
